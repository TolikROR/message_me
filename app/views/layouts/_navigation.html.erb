        <%= link_to 'Chatroom', root_path, class: "ui item" %>
        <a class="item">
          Messages
        </a>
        <div class="right menu">
          <div class="ui dropdown item">
            Account <i class="dropdown icon"></i>
            <div class="menu">
              
              <% if logged_in? %>
                <%= link_to 'Log out', logout_path, class: "ui item", method: :delete %>
              <% else %>
                <%= link_to 'Log in', login_path, class: "ui item" %>
                <%= link_to 'Sign up', signup_path, class: "ui item" %>
              <% end %>
            </div>
          </div>
          <div class="item">
            <% if logged_in? %>
              <%= link_to 'profile', user_path(current_user), class: "ui item" %>
            <% end %>
          </div>
        </div>